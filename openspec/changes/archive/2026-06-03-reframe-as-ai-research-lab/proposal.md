## Why

The repo has outgrown a single SSD-native inference prototype. It should become an AI research lab workspace with separate research tracks for the current SSD-native work, the next quality-gated stateful KV project, role-aware context compilation, and a living negative-space ideas archive.

## What Changes

- Move the current SSD-native inference work into its own research track folder.
- Add separate research folders for quality-gated stateful KV reuse and role-aware context compilation plus intentional recompute.
- Add a negative-space ideas folder for frontier scans and candidate research gaps.
- Update root documentation and agent guidance so the repo presents itself as an AI research lab.
- Rename the GitHub repository and local folder to `ai-research-lab` after the restructure lands.

## Capabilities

### New Capabilities

### Modified Capabilities

- `research-positioning-doc`: Expands the repo-level research positioning from one SSD-native project into a multi-track AI research lab structure.

## Impact

- Documentation and repository organization change.
- Existing current-track code, benchmarks, docs, and tests move under `research/01-ssd-native-inference-current/`.
- Root OpenSpec remains the lab-level change-management system.
- GitHub remote and local directory will be renamed after commit/push.
