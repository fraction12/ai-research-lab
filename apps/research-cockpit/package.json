{
  "name": "research-cockpit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "api": "python3 server.py --port 8766",
    "dev": "vite --host 127.0.0.1 --port 8765",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "preview": "vite preview --host 127.0.0.1 --port 8765"
  },
  "dependencies": {
    "@material/web": "^2.4.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
