## ADDED Requirements

### Requirement: Separate Code-mode runtime, KV capsule, and context-compilation claims
Track 02 positioning documentation SHALL separate model-facing Code-mode runtime-harness value, hidden KV capsule value, visible context-compilation value, and benchmark/source limitations.

#### Scenario: Reader checks non-handmade benchmark interpretation
- **WHEN** a reader opens the Track 02 benchmark summary or positioning note
- **THEN** the documentation identifies which claims are supported by non-handmade benchmark rows
- **AND** distinguishes handmade viability evidence, external benchmark evidence, direct-tool baseline gaps, compact-visible-evidence effects, native live append behavior, restored KV capsule behavior, and negative-control failures

#### Scenario: Reader drafts paper claim
- **WHEN** a reader drafts a paper-facing claim from the non-handmade benchmark result
- **THEN** the documentation provides allowed claims and disallowed claims based on the recorded control gates
