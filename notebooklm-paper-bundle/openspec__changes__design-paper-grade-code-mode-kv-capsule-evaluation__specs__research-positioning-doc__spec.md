## MODIFIED Requirements

### Requirement: Separate mechanisms in research positioning
Track 02 research positioning SHALL separate KV state reuse, Code-mode tool-surface compression, compact visible evidence scheduling, host repair, and fallback policy.

#### Scenario: Paper positioning is drafted
- **WHEN** the paper positioning or related-work section is drafted from the campaign
- **THEN** it states that the contribution is not a new KV cache primitive
- **AND** frames the contribution as a quality-gated local-agent runtime policy and evaluation protocol
- **AND** separately discusses hidden-state reuse, Code-mode interface compression, visible evidence ablations, host parsing/repair, and full-visible fallback

#### Scenario: Prior art is refreshed
- **WHEN** public paper claims are prepared
- **THEN** the prior-art scan is refreshed against stateful inference, KV cache reuse, agent context compression, tool-use benchmarks, and local-agent runtime work
- **AND** novelty claims remain blocked until that refresh is recorded
